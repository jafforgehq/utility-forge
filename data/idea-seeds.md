# Product Owner Idea Seeds

- Convert curl command -> fetch snippet generator
- JWT payload decoder and expiry checker
- HTTP status code lookup assistant
- Markdown table builder from CSV
- SQL formatter and pretty printer
- Cron expression explainer
- UUID generator and validator
- Color palette converter (HEX/RGB/HSL)
- URL query param diff tool
- Timezone converter for release planning
- Regex tester with named groups preview
- JSON schema starter generator
- Base64 / URL-safe converter
- OpenAPI endpoint snippet explorer
- Semver bump and comparison tool

